apiVersion: v1
kind: Service
metadata:
  name: hieudm17-capstone-service
spec:
  type: LoadBalancer
  selector:
    app: hieudm17-capstone-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
